#include <stdio.h>
#define TAM 100

int main (){
    char q[TAM];
    char s;[TAM];

    switch (q){
        case 0:
            if (s[TAM] == 'a' ) return 1;
            if (s[TAM] == 'b') return 2;
            break;
        case 1:
            if (s[TAM] == 'a') return 4;
            if (s[TAM] == 'b') return 5;
            break;
        case 2:
            if (s[TAM] == 'a') return 1;
            if(s[TAM] == 'b') return 4;
            break;
        case 4:
            if (s[TAM] == 'a') return 4;
            if (s[TAM] == 'b')return 4;
        case 5:
            if (s[TAM] == ' ') return -1;
            default printf("Programa encecesrrado")
    }


}